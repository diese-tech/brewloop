# Template reference

Unused starter UI primitives live here so the Black Rabbit prototype stays
focused without deleting potentially reusable template material.

Move a file back into `src` only when the active pilot imports it.
